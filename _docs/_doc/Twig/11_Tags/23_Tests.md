
### 🧭 **constant**

Bir değişkenin belirli bir PHP sabitiyle aynı değere sahip olup olmadığını kontrol eder.

### 🧩 **defined**

Bir değişkenin tanımlı olup olmadığını test eder (örnek: `{% if user is defined %}`).

### ➗ **divisible by**

Bir sayının başka bir sayıya kalansız bölünüp bölünmediğini kontrol eder.

### ⚪ **empty**

Bir değerin boş olup olmadığını kontrol eder (örnek: `null`, `[]`, `''` boş sayılır).

### 🔢 **even**

Bir sayının çift olup olmadığını test eder.

### 🔁 **iterable**

Bir değişkenin döngü içinde gezilebilir (iterable) olup olmadığını kontrol eder (örnek: dizi veya Traversable).

### 🗺️ **mapping**

Bir değişkenin “mapping” türünde (örneğin associative array veya object) olup olmadığını kontrol eder.

### 🚫 **null**

Bir değişkenin `null` olup olmadığını kontrol eder.

### 🔹 **odd**

Bir sayının tek olup olmadığını test eder.

### ⚖️ **same as**

Bir değerin diğerine **aynı nesne veya tam eşit** olup olmadığını kontrol eder (tip dâhil karşılaştırma yapar).

### 📜 **sequence**

Bir değişkenin sıralı (örneğin liste veya dizi) olup olmadığını test eder.
