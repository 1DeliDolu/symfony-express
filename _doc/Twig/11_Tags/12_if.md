**if** deyimi, Twig’de PHP’deki **if** deyimine benzer şekilde çalışır.

En basit haliyle, bir ifadenin **true** (doğru) olarak değerlendirilip değerlendirilmediğini test etmek için kullanılır:

```twig
{% if online == false %}
    <p>Our website is in maintenance mode. Please, come back later.</p>
{% endif %}
```

---

### 🔹 Boş Olmayan Diziler veya Eşlemeler

Bir dizi (sequence) veya eşlemenin (mapping) boş olup olmadığını da kontrol edebilirsiniz:

```twig
{% if users %}
    <ul>
        {% for user in users %}
            <li>{{ user.username|e }}</li>
        {% endfor %}
    </ul>
{% endif %}
```

> **Not:**
>
> Eğer bir değişkenin tanımlı olup olmadığını test etmek istiyorsanız, `if users is defined` kullanın.

---

### 🔹 Negasyon (not) Kullanımı

Bir değerin **false** olarak değerlendirilip değerlendirilmediğini kontrol etmek için **not** kullanılabilir:

```twig
{% if not user.subscribed %}
    <p>You are not subscribed to our mailing list.</p>
{% endif %}
```

---

### 🔹 Birden Fazla Koşul (and / or)

Birden fazla koşulu test etmek için **and** ve **or** kullanılabilir:

```twig
{% if temperature > 18 and temperature < 27 %}
    <p>It's a nice day for a walk in the park.</p>
{% endif %}
```

---

### 🔹 elseif ve else Kullanımı

PHP’de olduğu gibi, **elseif** ve **else** ifadelerini de kullanabilirsiniz.

Bu ifadelerde daha karmaşık koşullar da belirtilebilir:

```twig
{% if product.stock > 10 %}
   Available
{% elseif product.stock > 0 %}
   Only {{ product.stock }} left!
{% else %}
   Sold-out!
{% endif %}
```

---

### 🔹 Doğruluk (Boolean) Değerlendirme Kuralları

Twig, ifadelerin **true** veya **false** olarak değerlendirilmesinde PHP ile aynı kuralları uygular.

İşte bazı özel durumlar:

| Değer                              | Boolean Değeri |
| ----------------------------------- | --------------- |
| Boş string (`""`)                | false           |
| Sayısal sıfır (`0`)            | false           |
| `NAN`(Not A Number)               | true            |
| `INF`(Infinity)                   | true            |
| Sadece boşluklardan oluşan string | true            |
| `"0"`veya `'0'`stringi          | false           |
| Boş dizi (sequence)                | false           |
| Boş eşleme (mapping)              | false           |
| `null`                            | false           |
| Boş olmayan dizi                   | true            |
| Boş olmayan eşleme                | true            |
| Nesne (object)                      | true            |
