<form method="post" action="{{ path('app_job_delete', {'jobId': job.jobId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job.jobId) }}">
    <button class="btn">Delete</button>
</form>
