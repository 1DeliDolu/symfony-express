<form method="post" action="{{ path('app_sale_delete', {'store': sale.store}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sale.store) }}">
    <button class="btn">Delete</button>
</form>
