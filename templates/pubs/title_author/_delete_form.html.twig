<form method="post" action="{{ path('app_title_author_delete', {'author': title_author.author}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ title_author.author) }}">
    <button class="btn">Delete</button>
</form>
