<form method="post" action="{{ path('app_roysched_delete', {'title': roysched.title.titleId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ roysched.title.titleId) }}">
    <button class="btn">Delete</button>
</form>
