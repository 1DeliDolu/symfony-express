<form method="post" action="{{ path('app_author_delete', {'id': author.auId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ author.auId) }}">
    <button class="btn">Delete</button>
</form>
