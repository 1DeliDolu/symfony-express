<form method="post" action="{{ path('app_northwind_shippers_delete', {'id': shipper.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shipper.id) }}">
    <button class="btn">Delete</button>
</form>
