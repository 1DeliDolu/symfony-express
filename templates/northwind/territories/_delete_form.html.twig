<form method="post" action="{{ path('app_northwind_territories_delete', {'territoryId': territory.territoryId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ territory.territoryId) }}">
    <button class="btn">Delete</button>
</form>
