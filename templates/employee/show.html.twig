{% extends 'base.html.twig' %}

{% block title %}Employee{% endblock %}

{% block body %}
    <h1>Employee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>EmpId</th>
                <td>{{ employee.empId }}</td>
            </tr>
            <tr>
                <th>Fname</th>
                <td>{{ employee.fname }}</td>
            </tr>
            <tr>
                <th>Minit</th>
                <td>{{ employee.minit }}</td>
            </tr>
            <tr>
                <th>Lname</th>
                <td>{{ employee.lname }}</td>
            </tr>
            <tr>
                <th>JobLvl</th>
                <td>{{ employee.jobLvl }}</td>
            </tr>
            <tr>
                <th>HireDate</th>
                <td>{{ employee.hireDate ? employee.hireDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_employee_index') }}">back to list</a>

    <a href="{{ path('app_employee_edit', {'empId': employee.empId}) }}">edit</a>

    {{ include('employee/_delete_form.html.twig') }}
{% endblock %}
