<form method="post" action="{{ path('app_discount_delete', {'discountType': discount.discountType}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discount.discountType) }}">
    <button class="btn">Delete</button>
</form>
